dao:
  statements:
    findAll: SELECT * FROM ?

dynamic:
  tables:
    tasks:
      idColumn: task_id
      questionIdColumn: question_id
      performedTeamNameColumn: performed_team_name
    players:
      postfix: _players
      idColumn: tg_user_id
      teamNameColumn: team_name
